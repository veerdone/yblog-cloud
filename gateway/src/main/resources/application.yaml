server:
  port: 8082

spring:
  cloud:
    nacos:
      server-addr: localhost:8888
    gateway:
      routes:
        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/user/**
        - id: article-service
          uri: lb://article-service
          predicates:
            - Path=/article/**
  application:
    name: yblog-gateway